__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/
build/
dist/
*.egg-info/

# Runtime artifacts
logs/
state/

# Real config carries local paths + SMTP credentials. Never commit it.
# Ship config/nightshift.config.example.json only.
config/nightshift.config.json
*.local.json

# Private/project-specific plugins live outside the public repo. If you keep
# them in-tree for convenience, these directories are never committed.
plugins.local/
private/

.DS_Store
